<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Create Your UrbanMaid Account</title>
  <!-- Bootstrap and FontAwesome -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <!-- Your custom CSS files -->
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/navbar.css">
  <link rel="stylesheet" href="/css/register.css">
</head>
<body style="background: linear-gradient(120deg, #e0eefc 0%, #bad1ee 100%) no-repeat fixed;">
  <!-- Navbar -->
  <%- include('../partials/navbar', { user: user }) %>

  <div class="container py-5 min-vh-100 d-flex flex-column justify-content-center align-items-center">
    <a href="/" class="mb-4 d-inline-block text-decoration-none fw-semibold text-primary">
      <i class="fa-solid fa-arrow-left me-1"></i> Back to Home
    </a>
    <div class="card shadow-lg p-4 w-100" style="max-width: 430px; border-radius: 15px;">
      <div class="register-header text-center mb-4">
        <h1 class="mb-2 fs-3 fw-bold" style="color: var(--primary-blue);">Create Your UrbanMaid Account</h1>
        <p class="mb-0 text-muted">Sign up to find or offer services</p>
      </div>

      <!-- Flash Messages REMOVED -->

      <form class="register-form" method="POST" action="/auth/register" autocomplete="off">
        <div class="mb-3">
          <input name="name" type="text" class="form-control" placeholder="Full name *" required>
        </div>
        <div class="mb-3">
          <input name="email" type="email" class="form-control" placeholder="Email address *" required>
        </div>
        <div class="mb-3">
          <input name="password" type="password" class="form-control" placeholder="Password (min 6 chars) *" minlength="6" required>
        </div>
        <div class="mb-3">
          <input name="confirmPassword" type="password" class="form-control" placeholder="Confirm Password *" minlength="6" required>
        </div>
        <div class="mb-3">
          <select name="role" class="form-select" required>
            <option value="">I am a...</option>
            <option value="user">User (need services)</option>
            <option value="helper">Helper (offer services)</option>
          </select>
        </div>
        <div class="mb-3">
          <textarea name="about" rows="3" class="form-control" placeholder="Say something about yourself (optional)"></textarea>
        </div>
        <button class="btn btn-primary w-100 fw-bold py-2" type="submit" style="font-size: 1.15rem;">
          <i class="fa fa-user-plus me-1"></i> Register
        </button>
      </form>
      <div class="register-footer text-center mt-3">
        <small>Already have an account? <a href="/auth/login" class="text-primary fw-semibold text-decoration-underline">Login here</a></small>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS Bundle -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
